<form method="post" action="{{ path('app_viewer_delete', {'id': viewer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ viewer.id) }}">
    <button class="btn">Delete</button>
</form>

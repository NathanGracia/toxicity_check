<div class="container">
    <div class="row">
        <div class="col-md-6 ">
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.content, 'Contenu') }}
                {{ form_widget(form.content, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.toxicity, 'Toxicité') }}
                {{ form_widget(form.toxicity, {}) }}

                <div id="toxicityValue">{{ message.toxicity }} 0</div>
            </div>

            <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>

<script>
    console.log('oui')
    const toxicitySlider = document.getElementById('message_toxicity');
    console.log(toxicitySlider)
    const toxicityValue = document.getElementById('toxicityValue');

    toxicitySlider.addEventListener('input', function() {
        toxicityValue.innerHTML = toxicitySlider.value;
    });
</script>

